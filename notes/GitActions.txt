git add .
git commit -m "correction des fonctions avec ajout de bonus.css /malus.css pour visuel. implément des validator encore non fonctionnel"
git push

git add .
git commit -m " "
git push --set-upstream origin main

git push --set-upstream origin branche_créée

git commit --amend -m "nouveau message"

git log --online

git checkout n°commit